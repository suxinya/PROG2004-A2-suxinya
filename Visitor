public abstract class Visitor extends Person {
    // Private unique variables
    private String visitorId;     // Unique ID of tourist
    private String membershipType;// Membership Type

    // 1. Default constructor
    public Visitor() {
        super();
    }

    // 2. Constructor with parameters: parent class property+subclass property
    public Visitor(String personId, String name, int age, String visitorId, String membershipType) {
        super(personId, name, age);
        this.visitorId = visitorId;
        this.membershipType = membershipType;
    }

    // 3. Getter/Setter
    public String getVisitorId() {
        return visitorId;
    }

    public void setVisitorId(String visitorId) {
        this.visitorId = visitorId;
    }

    public String getMembershipType() {
        return membershipType;
    }

    public void setMembershipType(String membershipType) {
        this.membershipType = membershipType;
    }

    // Abstract method for implementing Person: displaying tourist information
    @Override
    public void showInfo() {
        System.out.println("=== Tourist Information ===");
        System.out.println("Tourist ID:" + getVisitorId());
        System.out.println("Name:" + getName());
        System.out.println("Age:" + getAge());
        System.out.println("Membership type:" + getMembershipType());
    }
}
